# Laptop-side pre-pod prep dependencies. Install this BEFORE renting an
# H100 to run the workflow described in RUNBOOK.md §3 Day 0 (locally).
#
# What this covers:
#   - LLM-narrator API call (default: gpt-5.4-nano; alt: claude-haiku-4-5).
#   - Template-only data generation (no API).
#   - The torch-free self-tests in RUNBOOK §0.1 / §0.2 Part A.
#   - The eval_modes / leakage_checks / bootstrap_ci modules.
#
# What this DOES NOT cover (deferred to the pod, see requirements.txt):
#   - transformers / accelerate / peft / bitsandbytes / safetensors.
#   - tokenizers / datasets.
#   - wandb.
#   - torch (provided by the RunPod base image).
#
# Why split: the pod stack is ~3 GB of GPU-oriented packages; pulling
# them onto a laptop just to run a $10 narrator job and a few smoke
# checks is wasted time and disk. Review 009 finding #4.

# LLM narrator providers (both kept import-safe — switch with LLM_PROVIDER).
openai>=1.50.0,<2.0
anthropic==0.39.0

# Synthetic-identifier generation (used by pii_fencer + journey templates).
faker==30.8.2

# Progress bars for build_dataset.py.
tqdm==4.66.6

# Config / state — used by the eval and template paths.
pyyaml==6.0.2
jsonlines==4.0.0
pydantic==2.9.2

# Eval/stats — needed by eval/bootstrap_ci.py and eval/score_risk.py if
# you want to validate the pre-pod templated eval set locally.
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
