Review comments 002 — follow-up
Date: 2026-05-15
Subject: Disposition of findings in review-comments-002.txt
Author: scaffold maintainer

Original review file:
/Users/arunmenon/projects/Foundation-Science/cross_attn_ato_poc/review/review-comments-002.txt

Repo state at follow-up:
/Users/arunmenon/projects/Foundation-Science/cross_attn_ato_poc/
Baseline commit: 67ee1e7

Summary:
All three findings from review-comments-002 were accepted and applied in
this pass. The verifications the reviewer ran (md5 of PLAN.md copies,
update_sweep_state smoke, pure-Python eval/leakage smoke, py_compile syntax
check) remain green; the doc/wording fixes do not change behavior.
Additionally, cross_attn_ato_poc is now a git repository with a baseline
commit, which closes the remaining concern about the agent's
git-checkpoint policy. Path A (build Day-1/2 implementation files locally)
begins after this follow-up.

Disposition per finding:

1. Minor: copy-paste verify commands should use python3 for this local
   shell.

Status: FIXED.

Action:
  - PLAN.md "Smoke tests" section now uses python3 in all local commands.
  - review/review-comments-001-followup.txt verify snippets updated to
    use python3 (replace_all: python -c -> python3 -c).
  - The smoke section was also restructured (see Finding 2) to make the
    "local" vs "pod" boundary explicit.

Verify (locally, in this shell):
    cd cross_attn_ato_poc
    python3 - <<'PY'
    import sys, random
    sys.path.insert(0, '.')
    from eval import eval_modes
    from eval.leakage_checks import narrative_leakage_scan
    text = '<journey_sim_swap><actor_human><event_login> <amount_bucket=high></journey_sim_swap>'
    assert 'journey_sim_swap' not in eval_modes.apply(text, 'stripped')[0]
    assert '<amount_bucket=high>' in eval_modes.apply(text, 'stripped')[0]
    a = eval_modes.apply(text, 'opaque', rng=random.Random(0))[0]
    b = eval_modes.apply(text, 'opaque', rng=random.Random(1))[0]
    assert a != b
    assert not narrative_leakage_scan('This is fraudulent SIM-swap')['clean']
    assert narrative_leakage_scan('Device change followed by password reset')['clean']
    print('layer A (local scaffold) smoke OK')
    PY

(Should print "layer A (local scaffold) smoke OK".)

2. Minor: PLAN.md labels a Day-0 block as "no GPU" while including
   preflight_check.py, which checks CUDA/VRAM.

Status: FIXED.

Action: the "Smoke tests" section in PLAN.md was restructured from two
layers (Day-0 / Day-1) into three explicit layers:

  Layer A — Local scaffold smoke (no GPU, no data, no model download;
            runnable today on any laptop).
            Contents: pure-Python eval_modes + narrative_leakage_scan
            assertions only.

  Layer B — Pod preflight (requires H100 + RunPod-like env).
            Contents: python3 scripts/preflight_check.py.
            Not runnable locally. Lives in the pod boot sequence
            (Day 0 / Hr 2-3) and gates Day-1 work.

  Layer C — Day-1 end-to-end (requires Day-1 modules: data generators,
            tokenizer registry, trainers).
            Contents: build_dataset, tokenizer check, leakage audit on a
            synthesized 100-row sample, accelerate launch, ad-hoc
            bootstrap_ci.

The previous coupling of preflight_check.py with the "no GPU" block was
the source of the confusion. The three-layer split makes the GPU
boundary explicit.

Verify: PLAN.md section "Smoke tests — three layers" reflects this
structure. All three copies of PLAN.md (in-repo, .claude/tasks/,
~/.claude/plans/) are md5-identical at hash 64515f2986ac4ea153e0b7b670d57c3e.

3. Informational: the repo directory is not itself a git repository.

Status: FIXED.

Action: cross_attn_ato_poc is now its own git repository (git init -b
main inside the directory). A baseline commit was made that includes the
full Day-0 scaffold:

    git log --oneline (in cross_attn_ato_poc):
    67ee1e7 Initial scaffold: Day-0 harness + v3 plan + reviews 001/002

The agent's git-checkpoint policy (AGENT_INSTRUCTIONS.md, RUNBOOK.md
section 7) now has a real git worktree to operate against. The pod boot
flow in RUNBOOK section 1.3 already assumes `git clone <your-repo-url>
repo` into /workspace, which puts cross_attn_ato_poc under /workspace/repo
where git operates. If the user pushes this directory to a remote and
clones it on the pod, the agent will be able to:

    git add -A && git commit -m "snapshot before <change description>"

…before any source-code edit, as the policy specifies.

Note on parent directory: /Users/arunmenon/projects/Foundation-Science is
intentionally not a git repository — it is the project root that houses
multiple independent code directories (cross_attn_ato_poc/,
sft_autoresearch/, .claude/tasks/, etc.). Initializing cross_attn_ato_poc
as a self-contained repo matches that pattern and avoids accidentally
versioning the broader project tree.

Verify:
    cd cross_attn_ato_poc
    git status
(Should report "On branch main" and "nothing to commit, working tree
clean".)

Side-effects to note (from reviewer's report):

The reviewer's verification step 2 refreshed src/auto_research/sweep_state.yaml
to the empty-state values. This is correct behavior — update_sweep_state()
is idempotent and reflects the current contents of experiments.jsonl
(which is also empty at this scaffold stage). When the first real
experiment runs, the launcher will rewrite sweep_state.yaml with the
populated state. The .gitignore correctly excludes
src/auto_research/sweep_state.yaml and experiments.jsonl from the repo,
so the empty-state file does not enter version control.

Path A status:

Per the open question at the end of review-comments-001-followup.txt,
the user has confirmed Path A: build the remaining Day-1/2 implementation
files locally before booting the H100. Files to author (per
review-comments-001.txt Finding 1):

  data/gen/build_dataset.py
  data/gen/journey_templates.py
  data/gen/agent_actor_mixer.py
  data/gen/feature_bucketer.py
  data/gen/pii_fencer.py
  data/gen/narrative_generator.py
  data/gen/cheap_template_generator.py
  src/tokenizer/custom_tokens.py
  src/tokenizer/fencer.py
  src/model/cross_attn_block.py
  src/model/resampler.py
  src/model/qwen_xattn_wrapper.py
  src/model/encoders/small_transformer.py
  src/train/train_cpt_light.py
  src/train/train_xattn.py
  src/train/train_lora_text_only.py
  src/train/train_structured_as_text.py
  src/train/train_event_only_classifier.py

Authoring order:

  Batch 1 — Tokenizer foundations (custom_tokens, fencer, feature_bucketer,
            pii_fencer). Foundational; everything downstream uses them.

  Batch 2 — Data generators (journey_templates, agent_actor_mixer,
            narrative_generator, cheap_template_generator, build_dataset).
            Consume tokenizer; produce datasets the trainers ingest.

  Batch 3 — Model architecture (small_transformer encoder, cross_attn_block,
            resampler, qwen_xattn_wrapper). The cross-attention surgery
            described in PLAN.md "Architecture".

  Batch 4 — Trainers (cpt_light, lora_text_only, structured_as_text,
            event_only_classifier, train_xattn). Consume tokenizer, data,
            and model; produce checkpoints and per-mode predictions.

Each batch will be smoke-tested against Layer A (where applicable) before
moving to the next. Each batch will end with a git commit so the agent's
git-revert path is well-defined.

Open question for the user:

None — Path A authorized, beginning Batch 1 in the next pass.
